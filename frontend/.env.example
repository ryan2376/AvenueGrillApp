# Avenue Grill frontend — environment template. Copy to .env.local for local dev.
# NEXT_PUBLIC_ vars are exposed to the browser — never put secrets here.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
